# ── Core automation ────────────────────────────────────────────
playwright==1.42.0          # Browser automation (pip install playwright && playwright install chromium)
pyyaml>=6.0.1               # Config file parsing

# ── Streamlit UI ───────────────────────────────────────────────
streamlit>=1.35.0           # Web app framework
pandas>=2.0.0               # DataFrame for results table

# ── AI layer (choose one or both) ──────────────────────────────
openai>=1.30.0              # OpenAI API client
anthropic>=0.25.0           # Anthropic Claude API client (fallback)

# ── Notifications (optional) ───────────────────────────────────
twilio>=9.0.0               # WhatsApp via Twilio (optional)

# ── Dev / optional ──────────────────────────────────────────────
python-dotenv>=1.0.0        # Load .env file for API keys

# ── REMOVED (no longer needed with Streamlit) ──────────────────
# APScheduler  — replaced by Streamlit's native rerun
# fastapi      — replaced by Streamlit UI
# uvicorn      — replaced by Streamlit server
